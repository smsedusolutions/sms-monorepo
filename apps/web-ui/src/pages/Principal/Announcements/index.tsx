/**
 * Principal Announcements page
 * The principal can view, create, and manage announcements school-wide.
 * Re-uses the full SchoolAdmin Announcements component since the feature set is identical.
 */
export { default } from '../../SchoolAdmin/Announcements';
